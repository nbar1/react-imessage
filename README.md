## React iMessage
This is just a small project to help me learn react that builds out a functional UI imitating iMessage. It is functional as in messages can be added to the UI, but does not in any way operate as an iMessage client.
